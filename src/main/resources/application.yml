server:
  port: 8888

spring:
  h2:
    console:
      enabled: true

keycloak:
  realm: PowerRanger
  auth-server-url: http://localhost:8080
  resource: springboot-keycloak
  public-client: true
  bearer-only: true
